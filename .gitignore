# Wrangler local dev secrets (ELEVENLABS_API_KEY, APP_PASSPHRASE, etc.)
.dev.vars
.dev.vars.*

# Wrangler / Node
.wrangler/
node_modules/

# Claude Code harness scratch (agent worktrees, local agent state) — never commit.
.claude/

# jsdom is installed transiently for tests (npm install --no-save jsdom);
# the app itself has zero dependencies and no build step.
package.json
package-lock.json

# Realtime-accuracy investigation scratch (Session 2) — local-only test harness,
# TTS corpus, and workflow script. Safe to delete after the investigation.
.dsp-test.mjs
.wf-realtime-research.mjs
.wf-accuracy.mjs
.test/
.tts/
.real/
tmp/
